export enum ENUM_PRODUCT_STATUS {
  DRAFTED = 'drafted',
  PUBLISHED = 'published',
  CANCELED = 'canceled',
}

export enum ENUM_PRODUCT_STOCK_CIRCULAR_STATUS {
  DRAFTED = 'drafted',
  PUBLISHED = 'published',
  CANCELED = 'canceled',
  CLOSED = 'closed',
  EXPIRED = 'expired',
}

export enum ENUM_PRODUCT_PRICE_CIRCULAR_STATUS {
  DRAFTED = 'drafted',
  PUBLISHED = 'published',
  CANCELED = 'canceled',
  CLOSED = 'closed',
  EXPIRED = 'expired',
}

export enum ENUM_PRODUCT_DISCOUNT_STATUS {
  DRAFTED = 'drafted',
  PUBLISHED = 'published',
  CANCELED = 'canceled',
  CLOSED = 'closed',
  EXPIRED = 'expired',
}

export enum ENUM_PRODUCT_SORT_OPTIONS {
  NEW = 'new',
  PRICE_ASC = 'price_asc',
  PRICE_DESC = 'price_desc',
  RATING = 'rating',
}

export enum ENUM_PRODUCT_CATALOGS_TYPE {
  DEPARTMENT = 'department',
  CATEGORY = 'category',
  SUB_CATEGORY = 'subCategory',
}
